# SyncFlow V2 Greenfield Agent Package

- **整体项目需要从零搭建。**
- 现有 PC / Mobile 的 v0 代码仅作为**视觉与交互参考资产**。
- PC 端和 Mobile 端都需要新建工程；只允许**人工参考**既有 UI 代码，不允许把它们当成当前产品的正式宿主仓库。
- Mobile 端因为最终是 **React Native + iOS 原生传输层**，所以即使视觉来自 React Web，代码层也必须重新实现。